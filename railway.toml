[build]
builder = "nixpacks"
buildCommand = "apt-get update && apt-get install -y git-lfs && git lfs install && git lfs pull && npm ci && npm run build && npm run copy-public"

[deploy]
startCommand = "node backend/server.js"
restartPolicyMaxRetries = 5
restartPolicyWindowMs = 60000
