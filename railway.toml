[build]
builder = "nixpacks"
buildCommand = "git lfs install && git lfs pull && npm ci && npm run build && npm run copy-public"

[deploy]
startCommand = "node backend/server.js"
restartPolicyMaxRetries = 5
restartPolicyWindowMs = 60000
